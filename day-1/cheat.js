'use strict';

// Create a program that writes this line 100 times:
// "I won't cheat on the exam!"

var times = 100;
var lineToPrint = "I won't cheat on the exam!";
for(var i = 0; i < times; i++) {
	console.log(lineToPrint);
}