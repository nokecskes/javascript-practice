'use strict';
// - Create a variable named `r` with the following content: `[54, 23, 66, 12]`
// - Print the sum of the second and the third element

var r = [54, 23, 66, 12];

function sum(a, b) {
  return a + b;
}

var sumOfElts = sum(r[1], r[2]);
console.log(sumOfElts);
